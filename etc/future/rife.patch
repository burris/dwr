Index: java/uk/ltd/getahead/dwr/DebugPageGenerator.java
===================================================================
RCS file: /cvs/dwr/java/uk/ltd/getahead/dwr/DebugPageGenerator.java,v
retrieving revision 1.2
diff -r1.2 DebugPageGenerator.java
42a43,70
> 
>     /**
>      * Generate the URL towards the JavaScript that needs to be included for
>      * a certain script name to be remoted
>      * @param contextPath TODO: define this outside of the servlet spec
>      * @param servletPath TODO: define this outside of the servlet spec
>      * @param scriptName The script to generate for
>      * @return An URL towards the JavaScript page
>      */
>     String generateInterfaceUrl(String contextPath, String servletPath, String scriptName);
> 
>     /**
>      * Generate the URL towards the JavaScript that needs to be included for
>      * the DWR engine to work
>      * @param contextPath TODO: define this outside of the servlet spec
>      * @param servletPath TODO: define this outside of the servlet spec
>      * @return An URL towards the JavaScript page
>      */
>     String generateEngineUrl(String contextPath, String servletPath);
> 
>     /**
>      * Generate the URL towards the JavaScript that needs to be included for
>      * DWR utilities to work
>      * @param contextPath TODO: define this outside of the servlet spec
>      * @param servletPath TODO: define this outside of the servlet spec
>      * @return An URL towards the JavaScript page
>      */
>     String generateUtilUrl(String contextPath, String servletPath);
Index: java/uk/ltd/getahead/dwr/impl/DefaultDebugPageGenerator.java
===================================================================
RCS file: /cvs/dwr/java/uk/ltd/getahead/dwr/impl/DefaultDebugPageGenerator.java,v
retrieving revision 1.4
diff -r1.4 DefaultDebugPageGenerator.java
94a95,109
>     public String generateInterfaceUrl(String contextPath, String servletPath, String scriptName)
>     {
>         return contextPath + servletPath + HtmlConstants.PATH_INTERFACE + scriptName + HtmlConstants.EXTENSION_JS;
>     }
> 
>     public String generateEngineUrl(String contextPath, String servletPath)
>     {
>         return contextPath + servletPath + HtmlConstants.FILE_ENGINE;
>     }
> 
>     public String generateUtilUrl(String contextPath, String servletPath)
>     {
>         return contextPath + servletPath + HtmlConstants.FILE_UTIL;
>     }
> 
106,108c121,123
<         String interfaceURL = contextPath + servletPath + HtmlConstants.PATH_INTERFACE + scriptName + HtmlConstants.EXTENSION_JS;
<         String engineURL = contextPath + servletPath + HtmlConstants.FILE_ENGINE;
<         String utilURL = contextPath + servletPath + HtmlConstants.FILE_UTIL;
---
>         String interfaceURL = generateInterfaceUrl(contextPath, servletPath, scriptName);
>         String engineURL = generateEngineUrl(contextPath, servletPath);
>         String utilURL = generateUtilUrl(contextPath, servletPath);

